
# Funcion  (X -> Dominio), (Y -> Codominio)

Es la relacion que existe entre dos conjuntos de elementos X y Y, que para unirlos pasa por una funcion F(X) y relacionas todos los valores del conjunto X con el Y.

# Dominio y Rango de una Funcion

Dominio: Conjunto de valores de x que puedes "poner" en la función sin que sea indefinida (por ejemplo, evitando divisiones por cero o raíces de negativos).
​

Rango: Conjunto de valores de salida f(x) que realmente se obtienen al evaluar la función en su dominio.



1. Notación de Conjuntos (La más directa) 📝Para decir "todos los números reales excepto el 3", se usa una barra diagonal o un signo de menos:$\mathbb{R} \setminus \{3\}$Esto se lee: "El conjunto de los Reales menos el número 3".2.

 Notación de Intervalos (La que mencionas del infinito) ♾️Esta es la que describe el "camino" de los números en la recta numérica. Como el $3$ es un agujero en el camino, tenemos que saltarlo. Usamos una U (que significa "Unión") para pegar los dos pedazos:$$(-\infty, 3) \cup (3, +\infty)$$$(-\infty, 3)$: Viene desde el menos infinito y llega justo antes del 3.$(3, +\infty)$: Empieza justo después del 3 y sigue hasta el infinito.Los paréntesis $(\,)$: Significan que el número no está incluido (el 3 queda fuera).



Para indicar que esta incluido en Intervalos se debe colocar una llave al lado del numero incluido y un parentesis para indicar que es sin incluir

# Restricciones

En una Raiz cuadrada no se pueden ingresar numeros negativos, lo cual seria R + (Todos los numeros reales positivos) -> (−∞,∞).

En una fraccion o cosiente el denominador nunca puede ser 0

La principal restricción de una función logarítmica es que su argumento debe ser mayor que cero (es decir, el valor dentro del logaritmo > 0), ya que no existe logaritmo de números negativos o cero en los reales.


# Función inyectiva (1nyectiva)

Es cuando los valores del conjunto de partida tiene como maximo 1 del de llegada (Y).


